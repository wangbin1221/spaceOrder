<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.imeeting">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".app.IMeetingApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="design_width_in_dp"
            android:value="360" />
        <meta-data
            android:name="design_height_in_dp"
            android:value="640" />

        <activity android:name=".modules.homepage.ui.activity.HomePageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".modules.search.ui.activity.SearchAllActivity"/>
        <activity android:name=".modules.search.ui.activity.SearchLocationActivity"/>
        <activity android:name=".modules.detail.ui.DeatilActivity"/>
        <activity android:name=".modules.login.ui.activity.LoginActivity"/>
        <activity android:name=".modules.story.ui.activity.StoryActivity"/>
        <activity android:name=".modules.story.ui.activity.StoryDetailActivity"/>
        <activity android:name=".modules.story.ui.activity.SearchStoryActivity"/>
    </application>

</manifest>